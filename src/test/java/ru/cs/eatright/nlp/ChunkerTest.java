package ru.cs.eatright.nlp;

import org.junit.Test;
//import ru.cs.eatright.nlp.signatures.Phrase;
//import ru.cs.eatright.nlp.signatures.Word;

import java.util.Arrays;
import java.util.Collections;
import java.util.List;

import static org.junit.Assert.*;

public class ChunkerTest {
/*
    private final Chunker chunker = new Chunker();

    @Test
    public void extractNounPhrases1() throws Exception {
        List<Phrase> expected = Arrays.asList(
                new Phrase(Collections.singletonList(new Word("вася", "NN"))),
                new Phrase(Arrays.asList(new Word("вкусную", "VB"), new Word("кашу", "NN")))
        );
        List<Phrase> actual = chunker.getPhrases("вася ест вкусную кашу");

        assertEquals(expected, actual);
    }

    @Test
    public void extractNounPhrases2() throws Exception {
        List<Phrase> expected = Arrays.asList(
                new Phrase(Arrays.asList(new Word("съем", "NN"), new Word("грибов", "NN"))),
                new Phrase(Arrays.asList(new Word("жареной", "NN"), new Word("картошкой", "NN"))),
                new Phrase(Collections.singletonList(new Word("курицей", "NN")))
        );
        List<Phrase> actual = chunker.getPhrases("съем грибов с жареной картошкой и курицей");

        assertEquals(expected, actual);
    } */
}
