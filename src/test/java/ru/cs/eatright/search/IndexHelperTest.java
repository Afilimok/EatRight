package ru.cs.eatright.search;

import org.junit.Test;
import java.util.HashSet;
import java.util.Set;

import static org.junit.Assert.*;

public class IndexHelperTest {
    /*
    @Test
    public void get2GramsByString() {
        Set<IndexKey> expected = new HashSet<>();
        expected.add(new IndexKey(0, "$м"));
        expected.add(new IndexKey(1, "мя"));
        expected.add(new IndexKey(2, "яс"));
        expected.add(new IndexKey(3, "со"));
        expected.add(new IndexKey(4, "о^"));
        assertEquals(expected, IndexHelper.getNGramsByString("мясо", 2));
    }

    @Test
    public void get3GramsByString() throws Exception {
        Set<IndexKey> expected = new HashSet<>();
        expected.add(new IndexKey(0, "$мя"));
        expected.add(new IndexKey(1, "мяс"));
        expected.add(new IndexKey(2, "ясо"));
        expected.add(new IndexKey(3, "со^"));
        assertEquals(expected, IndexHelper.getNGramsByString("мясо", 3));
    }*/
}