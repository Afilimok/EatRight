package ru.cs.eatright.search;

import org.junit.Test;
//import ru.cs.eatright.model.Product;
import java.util.Collections;
import java.util.Set;

import static org.junit.Assert.*;

public class IndexBuilderTest {
/*
    @Test
    public void createIndex() {
        Set<Product> products = Collections.singleton(new Product("мясо", 0, 0, 0, 0, 0, 0));

        Index index = new Index();
        index.update(new IndexKey(0, "$м"), products);
        index.update(new IndexKey(1, "мя"), products);
        index.update(new IndexKey(2, "яс"), products);
        index.update(new IndexKey(3, "со"), products);
        index.update(new IndexKey(4, "о^"), products);
        index.update(new IndexKey(0, "$мя"), products);
        index.update(new IndexKey(1, "мяс"), products);
        index.update(new IndexKey(2, "ясо"), products);
        index.update(new IndexKey(3, "со^"), products);
        assertEquals(index, IndexBuilder.createIndex(products));
    }
    */
}